# Pinned to the versions verified locally
Django==6.0.6
vaderSentiment==3.3.2
whitenoise==6.12.0
dj-database-url==3.1.2

# Postgres driver for Supabase (not used locally — local dev stays on SQLite).
psycopg[binary]>=3.2
